# Example secrets file for Streamlit Cloud
# Rename to secrets.toml and add your actual API keys

# Groq API key for cloud deployment
GROQ_API_KEY = "your_groq_api_key_here"

# Vector Database Configuration
vector_db = {
    "storage_type": "local",
    "persist_directory": "data/faiss_db",
    "dimension": 384,
    "metric_type": "inner_product"
}

# Add any other secrets your application needs here
# For example:
# api_key = "your_api_key_here"
# database_url = "your_database_url_here"
